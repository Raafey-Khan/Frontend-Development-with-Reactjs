/*

Promise

Promise.all()

The Promise.all() static method takes an iterable of promises as
input and returns a singal Promise. This returned promise fullfills when 
all of the input's promises. Fulfill (including when an empty iterable is
passed), with an array of the fullfillment values. It rejects when any 
of the input's promises rejects, with this first rejection reason.


it rejects if any of the input promises rejected



Promise.any()

how it works

when any input promie fillfill
it going to fulfill all the promise



Promise.race()



Promise.reject()
*/









