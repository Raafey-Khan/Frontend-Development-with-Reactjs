//  + unary plus
// Type of is also a unary operator
// cause it takes only one operand
/*

tries to convert the variable in
number if its not already a no
*/

let x = '22'

let y = +x;
console.log(typeof(y),typeof(x))

// It does't change the original operand 
//but instead return a converted value


// Crazy logic

let a = "-22";
let b = -a;
console.log(b) // answer will be in +22 cause - - will be plus